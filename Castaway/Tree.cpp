#include "Tree.h"

/*
	Constructor for Tree.
	@param treeName the tree's name
	@param treeDescription the description for the tree
	@param treeMoveable if the tree is moveable
*/
Tree::Tree(string treeName, string treeDescription, bool treeMoveable) : Item(treeName, treeDescription, treeMoveable)
{
}
